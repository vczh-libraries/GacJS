{
    "name": "@gaclib/renderer",
    "version": "0.0.1",
    "description": "GacUI HTML Renderer",
    "type": "module",
    "main": "./lib/src/index.js",
    "scripts": {
        "build": "rimraf ./lib && eslint && tsc",
        "test": "vitest run"
    },
    "license": "Apache-2.0",
    "devDependencies": {
        "eslint-shared": "^0.0.1"
    },
    "dependencies": {
        "@gaclib/remote-protocol": "^0.0.1"
    }
}